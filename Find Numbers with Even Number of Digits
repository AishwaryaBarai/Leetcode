class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        count = 0
        str_nums = map(str,nums)
        for s in str_nums:
            if len(s) %2 == 0:
                count +=1
        return count
